--- a/protocol/direct/outbound.go
+++ b/protocol/direct/outbound.go
@@ -99,7 +99,13 @@
 }
 
 func (h *Outbound) fetchMyAddresses() {
-	myInterfaceNames := h.network.InterfaceMonitor().MyInterfaces()
+	// Parvaz 2026-09-09: unprivileged Android may have no OS interface monitor.
+	// This child has only SOCKS listeners; Android owns the VPN TUN.
+	monitor := h.network.InterfaceMonitor()
+	if monitor == nil {
+		return
+	}
+	myInterfaceNames := monitor.MyInterfaces()
 	if len(myInterfaceNames) == 0 {
 		return
 	}
