android.useAndroidX=true
# Native helper packages three large Go ELF files. Zipflinger exhausted the
# standalone project's default 512 MiB heap (run35071841227, nested OOME).
org.gradle.jvmargs=-Xmx2048m -XX:MaxMetaspaceSize=512m
org.gradle.parallel=false
org.gradle.workers.max=2
