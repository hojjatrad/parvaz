# پرواز ۱.۲۸.۳ — اصلاح تست و نمایش تأخیر

- تست تکی، گروهی و HTTPS دستی از صف مشترک با حداکثر سه تست هم‌زمان استفاده می‌کنند؛ اشغال ظرفیت نباید کانکشن‌های انتهای صف را بی‌صدا حذف کند.
- عدد میلی‌ثانیه فقط برای پاسخ واقعاً اندازه‌گیری‌شده نمایش داده می‌شود. «تست تأخیر»، «در حال تست»، «تأیید نشد»، «ناموفق»، «مشغول» و «لغو شد» جدا هستند؛ نتیجهٔ نامشخص به قطع بودن سرور تعبیر نمی‌شود.
- لغو گروهی ردیف‌های منتظر را از حالت انتظار خارج می‌کند. نتیجهٔ دیررس، تست تازه‌تر یا نمونهٔ تأییدشدهٔ اتصال زنده را بازنویسی نمی‌کند؛ تغییر شبکه/پروفایل/منبع هم نتیجهٔ قبلی را بی‌اعتبار می‌کند.
- ذخیرهٔ تأخیر دیگر باعث بازنویسی اشتراک و تغییر revision آن نمی‌شود؛ ردیف تازه‌شده «در حال تست» قدیمی را به ارث نمی‌برد.
- حذف کانکشن‌های ناموفق فقط نتیجهٔ ناموفق را در نظر می‌گیرد، نه کانکشن تست‌نشده یا تأییدنشده.
- TLS، تأیید مسیر remote، تاریخچه/انتخاب منبع و امضای دائمی تغییر نکرده‌اند. تغییرات DNS آزمایشگاهی وارد این APK نشده‌اند.

این اعداد تأخیر درخواست HTTPS از مسیر پروکسی‌اند، نه ICMP یا تضمین سرعت دانلود. پیکربندی کاملِ مبهم ممکن است همچنان «تأیید نشد» بدهد؛ عدد ساختگی یا fallback مستقیم اضافه نشده است. اتصال VPN به‌تنهایی پینگ همهٔ کانکشن‌ها را اندازه نمی‌گیرد.

**وضعیت آزمون پیش از انتشار:** گیت‌های ساخت، رگرسیون، بسته‌بندی و امضای ثابت موفق شدند. بررسی فایل‌های عمومی پس از انتشار جدا ثبت می‌شود. تست نصب/عملکرد روی گوشی کاربر، نشت/Doze/باتری انجام نشده است. از پاک‌کردن داده یا حذف نسخهٔ نصب‌شده خودداری کنید.

## شواهد پیش از انتشار

- commit اجرایی آزموده‌شده: `7b7c588cb2197f70621bcbbfab86ac7e0f82f1d8`.
- 14 اجرای CI موفق؛ 414 تست واحد بدون failure/error/skip، از جمله 27 اجرای جدید برای نتیجه، صف، لغو، مالکیت و تغییر شبکه تا لحظهٔ نمایش.
- 15 آزمون native و 11 آزمون آرشیو/پشتیبان روی هر یک از SDK29/34؛ این‌ها آزمون شبیه‌سازند، نه گوشی کاربر.
- R8/lint، بستهٔ سورس و metadata رسمی سه هسته، امضای APK آماده روی runner جدا با کلید دائمی و 36 آزمون Python انتشار و audit میزبان موفق شدند.
- آزمایش DNS وارد نسخه نشده و آزمون نصب/عملکرد گوشی و شبکهٔ خصوصی NOT_RUN باقی می‌ماند.

## بررسی پس از انتشار

نسخهٔ stable و latest، هر دو APK، امضای دائمی، package/version/ABI، سورس کامل و هش‌های عمومی مستقل بررسی شدند. شواهد در [verification](v1.28.3-verification.json) و [وضعیت و محدودیت‌ها](../audit/LATENCY-DISPLAY-STATUS-fa.md) ثبت شده‌اند. نصب روی گوشی NOT_RUN است.
