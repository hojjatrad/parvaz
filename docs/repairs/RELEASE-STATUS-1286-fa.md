# وضعیت نسخهٔ ۱٫۲۸٫۶

**وضعیت انتشار: متوقف (BLOCKED). فایل APK نهایی هنوز تحویل یا منتشر نشده است.**

## موارد تأییدشده
- خطای کمبود حافظه در بسته‌بندی آزمون هسته‌ها اصلاح شد؛ اجرای35072640666: هجده آزمون اندروید۱۰ و هجده آزمون اندروید۱۴، بدون شکست/خطا/پرش.
- اجرای35073218654: تعداد۵۵۸ آزمون واحد، شامل۴۸ آزمون اصلاحات ممیزی، موفق؛ بررسی۸۷ وابستگی Java بدون تطابق شناخته‌شده در همان دامنه.
- ابزار آزمایش ارتقا با دکمهٔ واقعی برنامه و نصب‌کنندهٔ اندروید اضافه شده، اما هنوز اجرای موفق آن ثبت نشده است.
- مسیر انتشار دستی به موفقیت آزمون نصب، آزمون هسته‌ها، بازیابی داده، بررسی وابستگی‌ها و تطابق دقیق APK آزمایش‌شده وابسته شده است. کلید دائمی تغییر نکرده است.

## مانع انتشار
بررسی کامل گراف وابستگی‌ها در اجرای35073227338 برای۱۰ مورد از۷۵۸ مختصات بررسی‌شده هشدار ثبت کرد و پیش از امضا متوقف شد. بررسی تکمیلی فایل‌های هستهٔ نسخهٔ قبلی نشان داد که گراف منبع به‌تنهایی برای تشخیص نسخه‌های واقعاً بسته‌بندی‌شده کافی نیست. بررسی نسخهٔ واقعی کتابخانه‌ها، جایگزین‌ها/فورک‌ها و کاربرد توابع مشمول هشدار هنوز باید تکمیل شود.

این هشدارها به‌تنهایی اثبات سوءاستفاده‌پذیری برنامه نیستند. خروجی اسکن فایل‌های Go بدون نمادهای دیباگ نیز می‌تواند محافظه‌کارانه و در سطح کل ماژول باشد؛ نباید آن را اثبات اجرای تابع آسیب‌پذیر دانست. در عین حال، بدون تکمیل بررسی نمی‌توان آن‌ها را نادیده گرفت یا نسخه را آماده اعلام کرد.

## وضعیت گیت‌هاب و بروزرسانی
اصلاحات و شواهد در شاخهٔ `fix/audit-repairs` قرار دارند. نسخهٔ پایدار۱٫۲۸٫۵، تگ‌ها و فایل‌های قبلی دست‌نخورده‌اند. هنوز نسخهٔ جدیدی برای کشف و نصب از دکمهٔ بروزرسانی عمومی نشده است.

مرحلهٔ بعد: تعیین کاربرد واقعی هشدارها و تطابق وابستگی منبع/باینری، اصلاح لازم، اجرای دوبارهٔ آزمون‌ها، سپس آزمون APK امضاشده و بررسی تصاویر رابط. فقط پس از عبور از این مراحل، انتشار و تحویل APK جدید مجاز است. آزمون گوشی فیزیکی و همهٔ شرایط VPN/DNS انجام نشده و موفق گزارش نمی‌شود.
