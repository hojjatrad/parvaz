# برنامهٔ اجرایی موارد تأییدشدهٔ کاربر

نسخهٔ پایدار 1.26.3 تغییر نمی‌کند؛ نامزد 1.26.4 هنوز منتشر نشده است. gate امضاشدهٔ نامزد پیشین 95beaf2 در 34449253805 موفق بود؛ تغییرات این شاخه به gateهای تازه نیاز دارند.

| مورد | کار و معیار پذیرش | وضعیت |
|---|---|---|
| 1 | زمان‌سنج monotonic مراحل، گزارش دستی بدون URL/کلید، عدم اثر نتیجهٔ قدیمی | در برنامهٔ اجرا |
| 2 | تفکیک هستهٔ محلی از تأیید ارتباط؛ timeout به معنی تأیید یا قطع اجباری نباشد | در برنامهٔ اجرا |
| 3 | عدم انباشت START، توقف/جایگزینی و callbackهای قدیمی کنترل شوند | در برنامهٔ اجرا |
| 4 | مسیر bootstrap DNS، TTL و تغییر شبکه بررسی و با آزمون مستقل تأیید شود؛ بدون DNS مستقیم عمومیِ ناخواسته | نیازمند بررسی |
| 5 | حذف انتظار فقط با اثبات آزادشدن منابع؛ pooling/TLS resumption/MUX صرفاً برای پروتکل سازگار، نه اجباری عمومی | نیازمند بررسی |
| 6 | یک probe بد با ترافیک واقعی نباید قطع کند؛ عدم هم‌پوشانی health check و نتایج قدیمی | بخشی موجود؛ نیازمند آزمون/تقویت |
| 7 | انتخاب از منبع فعال، ترکیب سابقهٔ موفقیت/پایداری/latency، بودجهٔ محدود تست و لغو race | بخشی موجود؛ نیازمند آزمون/تقویت |
| 8 | چک‌لیست قابل اجرا روی گوشی برای Kill Switch، DNS/IPv6، Doze و handover؛ نتایج واقعی جدا ثبت شوند | اجرای فیزیکی در این محیط ممکن نیست |
| 9 | ادامهٔ فایل قطع‌شده فقط با هویت نسخه/URL/هش/حجم، بررسی Range، هش کامل و سپس امضا/بسته/نسخه؛ فایل ناقص هرگز نصب نشود | در حال پیاده‌سازی |

«موجود در کد»، «آزمون fixture»، «پروب Android» و «تأیید گوشی واقعی» معادل یکدیگر نیستند. این جدول تکمیل همهٔ موارد یا انتشار APK جدید را اعلام نمی‌کند.
