# وضعیت اجرای ۹ مورد تأییدشدهٔ کاربر

نسخهٔ **1.26.5/code32** اکنون روی stable/latest منتشر و بررسی عمومی شده است؛ تگ‌ها و فایل‌های قبلی دست‌نخورده‌اند. کد آزموده‌شده 72f8db8، تگ 6ad70e6 و workflow موفق 34480503105 هستند. جزئیات در `STATUS-v1.26.5-fa.md` و `../releases/v1.26.5-verification.json` آمده است.

## کار انجام‌شده و مرز ادعا

| مورد | آنچه اکنون انجام شده | باقی‌مانده / محدودیت |
|---|---|---|
| 1 ـ زمان مراحل | `StartupDiagnostics`: زمان monotonic ساخت VPN، نقاط داخل راه‌اندازی هسته، پاسخ HTTP از پراکسی محلی و اولین مشاهدهٔ RX پراکسی. گزارش فقط با انتخاب منوی «گزارش زمان اتصال»، در حافظه و بدون URL/اعتبارنامه/شناسهٔ پروفایل | DNS و نخستین پاسخ واقعی یک برنامه هنوز `NOT_INSTRUMENTED` هستند؛ RX شامل پروب/DNS هم هست. مدت cleanup داخل `CoreManager.start` جای کل زمان STOP/صف را نمی‌گیرد |
| 2 ـ وضعیت صادقانه | 1.26.5: پاسخ HTTPS با مسیر اثبات‌شده به سرور، ورودی جداگانه و pin پیش از DIRECT؛ رلهٔ native نیاز به اثبات مسیر داخلی دارد. شکست اول/deadline/busy دوباره بررسی می‌شود؛ یک worker و یک retry، backoff تا 60s، توقف پس از موفقیت؛ نتیجهٔ قدیمی حق سبزکردن ندارد | کانفیگ کامل/چندمسیرهٔ مبهم بدون انتخاب تصادفی حفظ می‌شود ولی بدون مدرک سبز نمی‌شود. این تأیید، سلامت همهٔ اپلیکیشن‌ها یا DNS را اثبات نمی‌کند؛ گوشی واقعی آزموده نشده |
| 3 ـ عملیات تکراری/قدیمی | یک worker چرخهٔ اتصال و حداکثر یک جایگزین در صف؛ START تکراری رد می‌شود؛ STOP/RESTART شمارهٔ مالکیت را عوض می‌کنند؛ reconnect دیررس و تکمیل قدیمی حق تغییر وضعیت ندارند. TUN مسدودکننده تا ساخته‌شدن جایگزین حفظ می‌شود | قطع native best-effort است؛ STOP برای آزادشدن منابع با کار جاری همگام می‌شود و تأخیر/ANR در شروعِ native کند باید روی Android واقعی بررسی شود |
| 4 ـ DNS/cache | مسیر تولید JSON بررسی و آزمون‌های cache روشن، DoH پیش‌فرض و routing DNS به proxy در حالت global افزوده شده‌اند؛ دور زدن جدید DNS/TLS یا resolver جاوا اضافه نشده | اندازه‌گیری مستقل DNS و انتقال TTL معتبر بین نمونه‌های native/تغییر شبکه هنوز پیاده نشده. `disableCache=false` اثبات نشت‌نداشتن یا TTL صحیح نیست |
| 5 ـ reconnect سازگار | حذف STARTهای زائد و صف‌های قدیمی، و لغو زودهنگام انتظار عملیات منسوخ. آزمون عدم MUX اجباری، غیرفعال‌بودن MUX با Vision و حذف MUX از relay موتور ثانویه | انتظار 300ms و debounce واقعی 1200ms باقی است. `ExternalCore.close` پس از destroyForcibly منتظر خروج نهایی نمی‌ماند؛ قبل از حذف انتظار باید lifecycle/بسته‌شدن پورت‌ها اثبات شود. pooling/TLS-resumption جدید یا MUX عمومی فعال نشده |
| 6 ـ تحمل وقفه | `HealthPolicy`: حداقل سه خطای endpoint برای core زنده، RX باعث پاک‌شدن strike می‌شود، TX تنها دلیل دریافت پاسخ نیست؛ هنگام RX از پروب اضافی صرف‌نظر می‌شود. health check هم‌پوشان و نتیجهٔ نشست قدیمی رد می‌شوند | آزمون باتری/Doze/شبکهٔ واقعی انجام نشده؛ RX شمارندهٔ پراکسی است نه تضمین هر برنامه |
| 7 ـ انتخاب فعال/محدود | race با `ProxyMeasurement` موتور واقعی HY2/TUIC/full config را به کار می‌برد؛ فقط snapshot منبع فعال با fingerprint یکسان، حداکثر سه probe در کل raceهای هم‌زمان، بازگشت زودهنگام هنگام شکست همه. صف native منسوخ انباشته نمی‌شود. سابقه فقط از نتایج مشاهده‌شده، با writer مشترک محافظت می‌شود | probeهای native ممکن است interrupt را نپذیرند؛ تا خروج واقعی slot خود را نگه می‌دارند. موفقیت probe، آماده‌بودن نشست VPN فعال نیست |
| 8 ـ دستگاه واقعی | چک‌لیست مستقل و ابزار اختیاری فقط‌خواندنی آماده است: `RELIABILITY-HARDWARE-fa.md` و `tools/android-probe/collect_readiness_environment.py` | **همهٔ تست‌های فیزیکی NOT_RUN**؛ اجرای ابزار در محیط حاضر `device_access=UNAVAILABLE` برگرداند |
| 9 ـ دانلود ادامه‌پذیر | cache به URL/نسخه/اندازه/SHA بسته است؛ Range صحیح append می‌شود، پاسخ 200 جایگزین می‌شود، 416 فقط یک بار از صفر تلاش می‌کند؛ قطع شبکه prefix را نگه می‌دارد. هش کامل، آرشیو، بسته، نسخه و امضا پیش از نام نهایی/نصب بررسی می‌شوند | آزمون واقعی نصب/قطع/ازسرگیری روی گوشی هنوز انجام نشده؛ cache توسط Android ممکن است پاک شود و سرور ممکن است Range را نپذیرد |

## شواهد اجراشده

جزئیات در `reliability-ci-evidence.json`:

- `1e71f1c` / Android **34452400222**: **226 tests / 0 failures / 0 errors / 0 skipped**؛ شامل 11 تست resume.
- `920b757` / Android **34453214618**: **249/0/0/0**؛ race، health و مالکیت SDK29/34.
- `7612ed2` / Android **34454138677**: **257/0/0/0**؛ 8 تست timing/readiness.
- `828c3b0` / Android **34455117477**: **265/0/0/0**؛ 8 تست صف اتصال و regressionهای قبل. Subscription **34455117639** موفق.
- آزمون‌های host refresh/dedup در هر مرحله: **53 assertion موفق**. این‌ها معیار رشدنکردن کانکشن‌ها هستند، نه تست امنیت فیزیکی.
- درخواست عمومی محدود به asset نسخهٔ پایدار، با offset=1024، پاسخ **206** و `Content-Range: bytes 1024-45376475/45376476` داد؛ فقط **1024 بایت** خوانده شد. مدرک در `public-apk-range-check.json`. این آزمایش نه دانلود کامل است، نه تأیید APK جدید.
- کد اجرایی نهایی `5d05c9c88f77dbb2d718003cd34d3b97d9f24182`: Android **34461057675** و دو اجرای تکراری، **277/0/0/0**؛ شامل 10 اجرای DNS/config و 12 مالکیت. gate امضا **34461057821** و پروب API29/34 **34461057682** موفق؛ جزئیات در `reliability-final-gates.json`. انتشار 34462806372 و بررسی عمومی هر چهار asset اکنون موفق‌اند.

**انتشار 1.26.4 تکمیل هر ۹ مورد، تأیید روی گوشی کاربر یا رفع همهٔ علت‌های تأخیر نیست.** Native DNS timing/cache migration، بهینه‌سازی lifecycle/pooling و آزمون‌های واقعی هنوز باز هستند. مسائل باقی‌ماندهٔ قبلی مانند updater روزانهٔ native و HY2 hopping نیز با این تغییرات تکمیل نشده‌اند.

- اصلاحیهٔ readiness در 1.26.5: **311/0/0/0** روی 72f8db8؛ هفت اجرای CI موفق و انتشار 34480503105؛ metadata، stream کامل چهار asset و امضای واقعی هر دو APK عمومی بررسی شدند. جزئیات در `../releases/v1.26.5-gates.json` و `../releases/v1.26.5-verification.json`. این اصلاح نیز تکمیل هر ۹ مورد نیست.
