> این گزارش مربوط به مرحلهٔ 1.19.1 است. وضعیت جدید در [گزارش 1.20](V1.20-RESULTS-fa.md) ثبت شده است.

# وضعیت امضا و انتشار — ۹ سپتامبر ۲۰۲۶

## وضعیت فعلی

- اصلاحات ورود کانفیگ و refresh تا مرحلهٔ B در GitHub موجودند؛ ۱۹۱ assertion میزبان مجدداً موفق شدند.
- کامپایل Android و تست‌های debug پایه در اجرای `34274426809` موفق شده‌اند؛ این نتیجه آزمون دستگاه نیست.
- تگ `v1.19` روی commit `b092484d436075799f59e9764861e25180d34acf` حفظ می‌شود. انتشار `34311459935` شکست خورد و هیچ APK یا Release جدیدی منتشر نشد.
- بررسی‌های `34311843710` و `34313818168` مشخص کردند چهار Secret به‌جای محتوای فایل، نام تنظیم را دارند؛ دادهٔ کلید در آن‌ها موجود نبود.
- فایل کلید نسل ۲ در مسیر مستثنا از ماندگاری نشست ذخیره شده بود و دیگر موجود نیست. این اشتباه نگهداری از طرف دستیار بود. مالک نیز پشتیبان را در دسترس نداشت.
- مالک سپس **صریحاً اجازهٔ ساخت کلید جایگزین** را داد. کلید نسل ۳ یک‌بار ساخته شد و باید برای تمام آپدیت‌های بعدی ثابت بماند.
- فایل خصوصی، رمزها، پشتیبان و فرم آفلاین کپی مقادیر خارج از مخزن و خارج از پوشه‌های مستثنا/موقت، با مجوزهای محدود ذخیره شده‌اند. هیچ‌کدام وارد Git نمی‌شوند. این محیط جایگزین بکاپ شخصی مالک نیست.
- بازشدن فایل، تطبیق گواهی، استفادهٔ واقعی از کلید خصوصی برای CSR و سلامت ZIP در محیط محلی بررسی شد.
- ابزار مشترک CI یازده تست واحد با دادهٔ مصنوعی دارد؛ حالت‌های نام‌به‌جای‌مقدار، Base64 خراب، رمز/alias نادرست و گواهی نامطابق را رد می‌کند و خروجی خصوصی keytool را چاپ نمی‌کند.

## نسخه و امضای جاری

- نسخهٔ منتشرشده: **1.19.1 / versionCode 20** به‌صورت Pre-release؛ تگ روی commit `6e4c1f57a93ab96d743ef97d3ab47e84a978e9a1` است.
- شناسهٔ برنامه: `com.parvaz.tunnel`، بدون تغییر.
- گواهی عمومی مرجع: `docs/signing/release-certificate.der`.
- SHA-256: `d1383b8f34da5d3299b13634de421487289d82c1bb47ec3bc7f20ae8d02fe500`.
- هیچ fallback به debug یا تولید خودکار کلید در workflow وجود ندارد.

## نتیجهٔ نهایی این مرحله

- مالک دریافت ZIP نسل ۳، بازشدن آن و جایگزینی چهار مقدار واقعی Secrets را تأیید کرد.
- فایل خصوصی و ZIP پس از ادامهٔ نشست نیز موجود و سالم بودند؛ کپی شخصی مالک همچنان پشتیبان اصلی لازم است.
- اجرای `34314502196`: کامپایل Android، تست‌های debug و بهینه‌سازی release با R8 موفق.
- اجرای فقط‌خواندنی `34314877310`: صحت واقعی رمز/alias/کلید خصوصی و گواهی، تست‌ها، ساخت APK امضاشده و بررسی امضا/شناسه/نسخه/هش موفق.
- اجرای انتشار `34315253198`: موفق؛ دو APK و فایل SHA256SUMS در GitHub به‌صورت **Pre-release** منتشر شدند.
- بایت‌های هر دو دانلود عمومی مجدداً خوانده و SHA-256 آن‌ها با checksum و metadata انتشار تطبیق داده شد؛ APKها در فضای محلی نگهداری نشدند.
- تگ قبلی v1.19 بازنویسی نشده و APK ندارد. نسخهٔ پایدار v1.18 و شاخهٔ main تغییر نکرده‌اند.
- ثبت عمومی نتیجه و هش‌ها: `docs/releases/v1.19.1-verification.json`.

صفحهٔ انتشار: https://github.com/hojjatrad/parvaz/releases/tag/v1.19.1

## باقی‌مانده برای تأیید پایدار

آزمون نصب، بازیابی تنظیمات، ورود کانفیگ‌های واقعی پنل‌ها و اتصال روی Android 10 به بالا هنوز انجام نشده است.
نسخهٔ آزمایشی را تا پایان این آزمون‌ها پایدار اعلام نکنید. نخستین نصب امضای جدید نیازمند پشتیبان‌گیری تنظیمات و مهاجرت از امضای قدیمی است؛ حذف/نصب روی گوشی انجام نشده است.
برای همهٔ آپدیت‌های بعدی همان کلید نسل ۳ و versionCode بالاتر لازم است؛ اجازهٔ جایگزینی این بار، مجوز چرخاندن کلید در آینده نیست.
