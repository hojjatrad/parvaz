name: Android archive and backup security probes
on:
  push:
    branches: ['ci/platform-update-probe']
permissions:
  contents: read
jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    strategy:
      fail-fast: false
      matrix:
        api: [29, 34]
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'
          cache-disabled: true
      - name: Fetch previously published signed APK as test data, never signing secrets
        run: |
          mkdir -p tools/android-probe/src/androidTest/assets
          curl -fL --max-time 120 https://github.com/hojjatrad/parvaz/releases/download/v1.26.1/Parvaz-1.26.1-arm64.apk -o tools/android-probe/src/androidTest/assets/published.apk
          echo 'f84f93a0ac9608826f68f85f7313c595dd710470674138538a13efafddfdc5d2  tools/android-probe/src/androidTest/assets/published.apk' | sha256sum -c
      - name: Enable emulator hardware acceleration
        run: |
          echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' | sudo tee /etc/udev/rules.d/99-kvm4all.rules
          sudo udevadm control --reload-rules
          sudo udevadm trigger --name-match=kvm
      - uses: reactivecircus/android-emulator-runner@v2
        with:
          api-level: ${{ matrix.api }}
          arch: x86_64
          target: default
          disable-animations: true
          script: gradle -p tools/android-probe --no-daemon --max-workers=2 connectedDebugAndroidTest
      - name: Expose public APK parser results
        if: always()
        run: |
          python3 - <<'PY'
          from pathlib import Path
          import xml.etree.ElementTree as ET
          base=Path('tools/android-probe/build/outputs/androidTest-results')
          for file in base.rglob('logcat*.txt'):
              for line in file.read_text(errors='replace').splitlines():
                  if 'I ParvazProbe:' in line:print('::notice title=ANDROID_ARCHIVE_PROBE::'+line)
          for file in base.rglob('*.xml'):
              root=ET.parse(file).getroot()
              print('::notice title=PLATFORM_TEST_COUNTS::'+root.get('name','')+' '+str({k:root.get(k,'0') for k in ['tests','failures','errors','skipped']}))
              for case in root.iter('testcase'):
                  if 'RestoreProcessDeathTest' in case.get('classname',''):
                      status='failed' if any(case.find(tag) is not None for tag in ['failure','error','skipped']) else 'passed'
                      print('::notice title=RESTORE_PROCESS_DEATH::'+case.get('name','')+' '+status)
              for item in root.iter('system-out'):
                  for line in (item.text or '').splitlines():
                      if 'ARCHIVE ' in line:print('::notice title=ANDROID_ARCHIVE_PROBE::'+line)
              for item in root.iter('failure'):print('::error title=ANDROID_ARCHIVE_PROBE::'+item.get('message','Assertion failed'))
          PY
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: archive-probe-${{ matrix.api }}
          path: tools/android-probe/build/outputs/androidTest-results/
          retention-days: 3
