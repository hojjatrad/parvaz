# نقشه‌ی راه پرواز — از «کار می‌کند» تا «حرفه‌ای»

بازنویسی‌شده بر پایه‌ی بررسی واقعیِ فایل نصبیِ **نسخه ۱.۵** (نه حدس و گمان).
همه‌ی موارد نقشه‌ی راه قبلی (نسخه ۱.۲) پیاده‌سازی و ارسال شده‌اند.

---

## وضعیت فعلی — بررسی‌شده از داخل APK

| بخش | وضعیت |
|---|---|
| هسته | Xray 26.7.31 |
| پروتکل‌ها | ✅ vmess · vless · trojan · shadowsocks · wireguard |
| پروتکل‌های غایب | ❌ hysteria2 · tuic · socks |
| قابلیت‌های موجود | QR، تفکیک اپ، فرگمنت TLS، بوت، ویجت، کاشی، قفل بیومتریک، WorkManager، نمودار مصرف، زنجیره‌ی پروکسی، قوانین سفارشی |
| حجم | ۳۵ مگابایت |
| **تست خودکار** | ❌ **هیچ** |
| **گزارش خطا** | ❌ **هیچ** |
| Always-on / Lockdown | ❌ مدیریت نشده |

از نظر «فهرست قابلیت‌ها» اپ تقریباً کامل است. چیزی که کم دارد **پایداری و
اطمینان** است — و این دقیقاً همان چیزی است که باگ نسخه ۱.۴ نشان داد.

---

## 🔴 اولویت صفر — درس نسخه ۱.۴ (مهم‌ترین بخش این سند)

یک اشتباه در **یک عدد** باعث شد کل بخش سرورها برای همیشه از کار بیفتد.
سه چیز باید اتفاق می‌افتاد و هیچ‌کدام نبود:

### ۰.۱ گزارش خطا (Crash Reporter) — مهم‌ترین مورد کل این سند
الان هیچ `UncaughtExceptionHandler` در اپ نیست. یعنی وقتی برنامه می‌بندد،
**هیچ ردی باقی نمی‌ماند** و من مجبور شدم باگ ۱.۴ را از روی حدس و بازخوانی کد پیدا کنم.

راه‌حل (بدون سرور و بدون نقض حریم خصوصی):
- گرفتن خطا، ذخیره‌ی stack trace در فایل محلی
- موقع باز شدن بعدی: «برنامه بار قبل بسته شد — گزارش را کپی/ارسال می‌کنید؟»
- دکمه‌ی کپی یا اشتراک‌گذاری در تلگرام

**اثر: خیلی زیاد.** با این، هر باگ آینده در ۳۰ ثانیه پیدا می‌شود نه ۳ ساعت.
> تخمین: ۲ ساعت

### ۰.۲ حالت امن (Safe Mode)
اگر اپ سه بار پشت سر هم موقع باز شدن بست، خودکار با لیست سرورهای خالی
بالا بیاید و بپرسد «بازیابی کنم؟». همان وضعیت «چسبنده»‌ای که تجربه کردید
دیگر هرگز کاربر را گیر نمی‌اندازد — مستقل از اینکه باگ چه باشد.
> تخمین: ۲ ساعت · اثر: زیاد

### ۰.۳ تست خودکار برای منطق حساس
در پروژه **هیچ تستی وجود ندارد**. یک تست ساده روی `FlagUtil.toFlag("DE")`
باگ ۱.۴ را در همان لحظه‌ی ساخت می‌گرفت.

حداقلِ ارزشمند — تست واحد (بدون نیاز به گوشی) روی:
- `FlagUtil` (پرچم‌ها)
- `LinkParser` (هر ۵ پروتکل + ورودی خراب/خالی/نامعتبر)
- `Profile.toJson/fromJson` (رفت‌وبرگشت کامل)
- `ProfileStore` (داده‌ی معیوب نباید لیست را بترکاند)

> تخمین: ۴ ساعت · اثر: خیلی زیاد
> این تنها موردی است که **جلوی تکرار** فاجعه‌ی ۱.۴ را می‌گیرد.

### ۰.۴ تست دود (Smoke Test) قبل از هر تحویل
یک اسکریپت که بعد از هر بیلد، خودکار چک کند: امضا درست، نسخه درست،
کلاس‌های کلیدی موجود، و مقادیر ثابتِ حساس سر جایشان.
> تخمین: ۱ ساعت

---

## اولویت ۱ — پایداری و اطمینان کاربر

### ۱.۱ مدیریت Always-on VPN و Lockdown
منیفست هیچ اشاره‌ای به این ندارد. اگر کاربر در تنظیمات اندروید
«VPN همیشگی» را روشن کند، رفتار اپ نامشخص است.
- تشخیص `isAlwaysOn` / `isLockdownEnabled` و نمایش وضعیت
- شروع درست سرویس بدون باز شدن اپ
> تخمین: ۲ ساعت · اثر: زیاد (امنیت واقعی)

### ۱.۲ اتصال مجدد خودکار روی تغییر شبکه
وقتی از Wi-Fi به دیتا می‌روید، اتصال معمولاً می‌میرد.
`NetworkCallback` + برقراری دوباره‌ی خودکار.
> تخمین: ۳ ساعت · اثر: خیلی زیاد در استفاده‌ی روزمره

### ۱.۳ محافظت از پشتیبان‌گیری خودکار اندروید
`allowBackup` روشن است و `dataExtractionRules` تعریف نشده. یعنی کانفیگ‌ها
(شامل UUID و کلیدها) ممکن است در بکاپ ابری گوگل کپی شوند.
باید یا خاموش شود یا قواعدش دقیق تعریف شود.
> تخمین: ۳۰ دقیقه · اثر: زیاد (حریم خصوصی)

### ۱.۴ پیام خطای معنادار هنگام عدم اتصال
الان «خطا» می‌گوید. باید بگوید *چرا*: احراز هویت رد شد / تایم‌اوت /
DNS جواب نداد / سرور دست‌نیافتنی. با پیشنهاد راه‌حل.
> تخمین: ۳ ساعت · اثر: زیاد

---

## اولویت ۲ — قدرت بیشتر در دور زدن فیلترینگ

### ۲.۱ پروتکل‌های Hysteria2 و TUIC
بزرگ‌ترین شکاف فنی باقی‌مانده. این‌ها مبتنی بر QUIC/UDP هستند و در شرایط
فیلترینگ شدید معمولاً بهتر از TCP دوام می‌آورند. نیازمند افزودن هسته‌ی
`sing-box` در کنار Xray است.
> تخمین: ۱–۲ روز · اثر: بالقوه خیلی زیاد — **بستگی به کانفیگ‌های شما دارد**
> اگر فروشنده‌ی شما hysteria2 نمی‌دهد، ارزش این کار صفر است. اول بپرسید.

### ۲.۲ چرخش خودکار بین سرورها هنگام قطعی
اگر سرور فعلی افت کرد، خودکار سراغ بعدی برود (الان جابه‌جایی هست ولی
می‌تواند هوشمندتر و بی‌وقفه‌تر شود).
> تخمین: ۳ ساعت

### ۲.۳ تست «آیا واقعاً کار می‌کند؟»
پینگ پایین یعنی سرور زنده است، نه اینکه فیلتر رد می‌شود. یک تست واقعی:
باز کردن یک سایت مسدود از طریق پروکسی و اعلام موفقیت/شکست.
> تخمین: ۲ ساعت · اثر: زیاد

---

## اولویت ۳ — صیقل حرفه‌ای

### ۳.۱ کاهش حجم از ۳۵ به حدود ۱۲ مگابایت
بررسی دقیق حجم فعلی:

| بخش | حجم | سهم |
|---|---|---|
| کتابخانه‌ی ۳۲بیتی (armeabi-v7a) | ۱۱.۷ MB | ۳۴٪ |
| کتابخانه‌ی ۶۴بیتی (arm64) | ۱۱.۳ MB | ۳۲٪ |
| geoip.dat + geosite.dat | ۷.۰ MB | ۲۰٪ |
| کد (dex) | ۳.۷ MB | ۱۱٪ |

- نسخه‌ی جدا برای arm64 (تقریباً همه‌ی گوشی‌های امروزی): **−۱۲ MB**
- دانلود دیتای geo در اولین اجرا به‌جای بسته‌بندی: **−۷ MB**
- فعال کردن R8/minify (الان خاموش است): **−۲ MB**
> تخمین: ۳ ساعت · اثر: زیاد برای دانلود و نصب

### ۳.۲ فعال کردن R8 + بهینه‌سازی
الان `minify off` است. علاوه بر حجم، سرعت اجرا هم بهتر می‌شود.
نیازمند قواعد ProGuard دقیق برای Xray است.
> تخمین: ۲ ساعت

### ۳.۳ به‌روزرسانی داخل برنامه
الان هر نسخه را باید دستی بفرستم و شما دستی نصب کنید. یک بررسی ساده‌ی
نسخه از یک آدرس + اعلان «نسخه جدید آمد».
> تخمین: ۳ ساعت · اثر: زیاد برای شما و من

### ۳.۴ صفحه‌ی اول هوشمندتر
نمایش «آخرین سرور موفق»، مصرف امروز، و وضعیت سلامت در یک نگاه.
> تخمین: ۲ ساعت

### ۳.۵ دسترس‌پذیری و RTL کامل
بازبینی اندازه‌ی لمس، توضیح صوتی برای دکمه‌ها، و چیدمان RTL در حالت‌های لبه‌ای.
> تخمین: ۲ ساعت

---

## 🎯 پیشنهاد من برای نسخه ۱.۶

اگر فقط یک بسته انتخاب کنم، **روی پایداری تمرکز می‌کنم، نه قابلیت جدید**.
دلیلش ساده است: اپ از نظر قابلیت‌ها الان کامل است، ولی نسخه ۱.۴ ثابت کرد
که تور ایمنی ندارد.

| # | مورد | چرا | زمان |
|---|---|---|---|
| ۱ | گزارش خطا | بدون این، هر باگ بعدی هم کورکورانه پیدا می‌شود | ۲ س |
| ۲ | تست خودکار | تنها چیزی که جلوی تکرار باگ ۱.۴ را می‌گیرد | ۴ س |
| ۳ | حالت امن | کاربر دیگر هرگز گیر نمی‌افتد | ۲ س |
| ۴ | اتصال مجدد روی تغییر شبکه | محسوس‌ترین آزار روزمره | ۳ س |
| ۵ | محافظت از بکاپ | نشت کلیدها به ابر گوگل | ۰.۵ س |

**مجموع: حدود ۱۲ ساعت.** نتیجه: اپی که وقتی خراب می‌شود، خودش می‌گوید کجا.

بعد از آن، نسخه ۱.۷ می‌تواند سراغ کاهش حجم و hysteria2 برود.

---

## دو نکته‌ی صادقانه

**۱. درباره‌ی hysteria2:** قبل از اینکه یک تا دو روز روی آن کار کنم،
بگویید فروشنده‌ی کانفیگ شما اصلاً این پروتکل را می‌دهد یا نه. اگر نمی‌دهد،
این کار هیچ سودی برای شما ندارد.

**۲. درباره‌ی تست:** در این محیط شبیه‌ساز اندروید وجود ندارد. من فقط
کامپایل، صحت امضا و درستی بایت‌کد را می‌توانم تأیید کنم. به همین دلیل
مورد ۰.۳ (تست واحد) این‌قدر مهم است — تست‌های واحد **بدون گوشی** اجرا
می‌شوند و تنها راه من برای گرفتن باگ قبل از رسیدن به دست شماست.
